{% extends 'AbleSpecAdminBundle::base.html.twig' %}

{% block body -%}
    <h3>{% trans %}Package edit{% endtrans %}</h3>

    {{ form(edit_form) }}

    <ul class="nav nav-pills record_actions">
        <li class="active">
            <a href="{{ path('package') }}">
                {% trans %}Back to the list{% endtrans %}
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
